# 项目深挖 测试一下

## proj-001

### Q1 single | Webpack 核心流程

Webpack 构建的核心主线更准确的是哪一个？

- [x] A. 从 entry 构建模块图，再生成 chunk graph，最终输出 assets。
- [ ] B. 先输出文件，再反向寻找入口。
- [ ] C. 只压缩 CSS，不处理模块依赖。
- [ ] D. 直接运行浏览器 DOM。

**解释**：Webpack 的核心是模块依赖分析和产物生成，entry、module graph、chunk、asset 是主线。

### Q2 multiple | Compiler 与 Compilation

哪些说法正确？

- [x] A. Compiler 代表整个 Webpack 实例。
- [x] B. Compilation 代表一次具体构建过程。
- [x] C. watch 模式下多次变更会产生多次 Compilation。
- [x] D. Compilation 中包含模块、依赖、chunk、assets、warning/error 等状态。
- [ ] E. Compiler 和 Compilation 完全是同一个对象。

**解释**：插件开发和构建排查经常要区分全局生命周期和单次构建上下文。

### Q3 multiple | Make 与 Seal

哪些描述正确？

- [x] A. Make 阶段从 entry 出发解析模块和依赖。
- [x] B. Loader 会在模块构建中转换源码。
- [x] C. Parser 解析 AST 并收集 import、require、动态导入等依赖。
- [x] D. Seal 阶段会生成 chunk、做优化并准备 assets。
- [ ] E. Emit 发生在 entry 解析之前。

**解释**：Make 负责“建图”，Seal 负责“封装和优化”，Emit 才是写出产物。

## proj-002

### Q1 single | Loader 和 Plugin

Webpack Loader 和 Plugin 的核心区别是什么？

- [x] A. Loader 转换单个模块内容，Plugin 通过钩子介入构建生命周期。
- [ ] B. Loader 只能处理图片，Plugin 只能处理 CSS。
- [ ] C. 二者完全一样。
- [ ] D. Plugin 不能访问构建产物。

**解释**：Loader 是模块级转换管线，Plugin 是构建生命周期扩展机制。

### Q2 multiple | 自定义 Loader

编写自定义 Loader 时，哪些说法正确？

- [x] A. 输入通常是源码字符串或 Buffer。
- [x] B. 输出应是转换后的模块代码或通过 callback 异步返回。
- [x] C. Loader 可通过 options 接收配置。
- [x] D. 多个 normal loader 通常从右到左执行。
- [ ] E. Loader 应直接把最终文件写入 output.path。

**解释**：Loader 负责模块转换，不应绕过 Webpack 的模块和 asset 管线随意写文件。

### Q3 multiple | 自定义 Plugin

编写 Plugin 时，哪些做法正确？

- [x] A. 暴露 `apply(compiler)` 方法。
- [x] B. 通过 compiler 或 compilation hooks 注册逻辑。
- [x] C. Webpack 5 修改 assets 推荐使用 `compilation.hooks.processAssets`。
- [x] D. 异步钩子要正确返回 Promise 或调用 callback。
- [ ] E. Plugin 只能在运行时浏览器里执行。

**解释**：Plugin 运行在构建阶段，借助 Tapable hooks 读写构建状态和产物。

## proj-003

### Q1 single | 优化顺序

优化 Webpack 构建速度和产物体积前，最应该先做什么？

- [x] A. 用数据分析构建耗时和 bundle 组成。
- [ ] B. 盲目替换所有依赖。
- [ ] C. 删除所有测试。
- [ ] D. 关闭版本控制。

**解释**：构建速度和产物体积是两个问题，先定位瓶颈，避免无效优化。

### Q2 multiple | 构建速度优化

哪些手段可能提升构建速度？

- [x] A. 缩小 loader 处理范围。
- [x] B. 开启持久化缓存。
- [x] C. 使用 esbuild/swc 处理部分转译或压缩。
- [x] D. 类型检查和 lint 在 CI 或独立进程处理。
- [ ] E. 每次构建都清空所有缓存。

**解释**：速度优化重点是减少重复工作、缩小范围和使用更快工具。

### Q3 multiple | 产物体积优化

哪些手段可优化产物体积？

- [x] A. Tree Shaking 和 sideEffects 配置。
- [x] B. 代码分割。
- [x] C. 按需引入或替换重依赖。
- [x] D. 去除重复 polyfill 和未使用资源。
- [ ] E. 把所有页面依赖都打进首屏 bundle。

**解释**：体积优化要减少无用、重复和首屏不需要的代码。

## proj-004

### Q1 single | Vite 开发快

Vite 开发模式为什么通常比传统 Webpack dev 冷启动快？

- [x] A. 利用浏览器原生 ESM，按需转换源码模块。
- [ ] B. 每次启动都完整打包全部应用。
- [ ] C. 不需要开发服务器。
- [ ] D. 不解析任何依赖。

**解释**：Vite dev 避免启动前整体打包，依赖预构建和按需模块加载提高速度。

### Q2 multiple | Vite 原理

哪些说法正确？

- [x] A. 依赖预构建常用 esbuild。
- [x] B. 源码模块按浏览器请求即时转换。
- [x] C. HMR 可以只更新变更模块及其边界。
- [x] D. 生产构建通常仍基于 Rollup。
- [ ] E. Vite 开发和生产完全不需要任何打包思路。

**解释**：Vite 开发与生产目标不同：开发重即时反馈，生产重优化产物。

### Q3 multiple | 选择 Vite 的边界

哪些因素会影响是否选择 Vite？

- [x] A. 生态插件是否满足项目需求。
- [x] B. 老项目迁移成本。
- [x] C. 依赖是否有复杂 CommonJS 或特殊构建假设。
- [x] D. 团队调试和部署链路。
- [ ] E. Vite 一定适合所有历史项目且零迁移成本。

**解释**：工具选型要结合生态、迁移、团队和边界场景，而不是只看基准速度。

## proj-005

### Q1 single | 微前端目标

微前端主要解决什么问题？

- [x] A. 多团队、多应用在一个产品中独立开发、部署和集成。
- [ ] B. 自动让页面没有任何 JS。
- [ ] C. 替代所有后端接口。
- [ ] D. 禁止模块拆分。

**解释**：微前端关注组织和系统拆分，代价是运行时集成、隔离和治理复杂度。

### Q2 multiple | qiankun 原理

qiankun 一类微前端方案通常包含哪些能力？

- [x] A. 主应用注册和加载子应用。
- [x] B. HTML Entry 解析子应用资源。
- [x] C. JS 沙箱隔离全局变量污染。
- [x] D. 样式隔离或约束方案。
- [ ] E. 自动消除所有跨应用通信设计。

**解释**：微前端框架提供加载和隔离基础，但应用通信、权限和路由仍需设计。

### Q3 multiple | 微前端风险

哪些是微前端常见风险？

- [x] A. 公共依赖重复加载。
- [x] B. 样式冲突和全局变量污染。
- [x] C. 应用间路由、状态和权限边界复杂。
- [x] D. 子应用独立部署导致版本兼容问题。
- [ ] E. 微前端一定比单体应用更简单。

**解释**：微前端适合组织复杂度高的场景，小项目盲目引入会增加成本。

## proj-006

### Q1 single | 错误监控目标

前端错误监控的核心目标是什么？

- [x] A. 发现、聚合、定位线上用户遇到的错误。
- [ ] B. 替代所有单元测试。
- [ ] C. 删除所有 try/catch。
- [ ] D. 自动修复后端服务。

**解释**：错误监控是线上质量闭环的一部分，帮助发现本地无法覆盖的问题。

### Q2 multiple | 捕获错误类型

前端错误监控应覆盖哪些类型？

- [x] A. JavaScript 运行时错误。
- [x] B. Promise 未处理 rejection。
- [x] C. 资源加载错误。
- [x] D. 框架错误边界捕获的组件错误。
- [x] E. 接口异常和业务错误码。

**解释**：错误来源包括运行时、异步、资源、框架和接口层，不能只监听 window.onerror。

### Q3 multiple | 上报质量

哪些信息有助于定位错误？

- [x] A. 错误栈和 source map 还原结果。
- [x] B. 页面 URL、路由、版本号和用户环境。
- [x] C. 最近操作路径或关键 breadcrumbs。
- [x] D. 采样、去重和聚合规则。
- [ ] E. 用户完整密码和隐私字段。

**解释**：定位需要上下文，但必须脱敏，避免监控系统变成隐私泄漏源。

## proj-007

### Q1 single | 声明式埋点

声明式埋点更接近哪种方式？

- [x] A. 在配置或 DOM/组件属性中描述埋点，由框架统一采集。
- [ ] B. 每次点击都手写一段 fetch。
- [ ] C. 完全不需要事件定义。
- [ ] D. 只采集服务端日志。

**解释**：声明式埋点降低业务代码侵入，但需要统一规范和采集框架。

### Q2 multiple | 埋点系统设计

设计埋点系统时，哪些点重要？

- [x] A. 事件命名和参数 schema。
- [x] B. 触发时机和去重规则。
- [x] C. 批量、采样和失败重试。
- [x] D. 隐私合规和脱敏。
- [ ] E. 允许各业务随意命名字段且不记录文档。

**解释**：埋点最怕口径混乱。事件治理和数据质量比简单上报更重要。

### Q3 multiple | 命令式埋点

命令式埋点更适合哪些场景？

- [x] A. 复杂业务条件下才上报的事件。
- [x] B. 需要携带计算后业务字段的事件。
- [x] C. 跨多个步骤完成后上报的转化事件。
- [ ] D. 所有简单按钮曝光都必须手写命令式代码。
- [ ] E. 命令式埋点天然不会漏报。

**解释**：命令式灵活但侵入强，声明式规范但对复杂业务表达能力有限。

## proj-008

### Q1 single | Web Vitals 采集

在项目中采集 LCP、INP、CLS，最常用的思路是什么？

- [x] A. 使用 web-vitals 或 PerformanceObserver 采集并上报。
- [ ] B. 让用户手动填写表单。
- [ ] C. 只看服务端 CPU。
- [ ] D. 读取 CSS 文件大小作为全部指标。

**解释**：Web Vitals 需要在浏览器端采集真实用户体验数据。

### Q2 multiple | 采集维度

上报 Web Vitals 时，哪些维度有助于分析？

- [x] A. 页面路由和版本。
- [x] B. 设备类型、网络类型和浏览器。
- [x] C. LCP 元素信息或资源 URL。
- [x] D. 是否首次访问、缓存状态等上下文。
- [ ] E. 用户身份证原文。

**解释**：性能分析需要维度，但应避免敏感信息。

### Q3 multiple | 指标治理

如何让 Web Vitals 监控真正有用？

- [x] A. 看真实用户第 75 百分位。
- [x] B. 建立阈值和告警。
- [x] C. 按页面和人群分维度排查。
- [x] D. 将优化结果回归到监控趋势。
- [ ] E. 只在上线前本地跑一次 Lighthouse。

**解释**：性能治理需要持续监控和闭环，不是一次性跑分。

## proj-009

### Q1 single | CI/CD 主线

完整前端 CI/CD 流程的主线是什么？

- [x] A. 提交代码 -> 检查/测试 -> 构建 -> 产物发布 -> 部署验证。
- [ ] B. 手动复制 dist 到每台电脑。
- [ ] C. 先部署再 lint。
- [ ] D. 只运行 prettier。

**解释**：CI/CD 的目标是用自动化流程保证质量、可重复构建和可靠发布。

### Q2 multiple | CI 阶段

哪些步骤适合放在 CI 中？

- [x] A. 类型检查和 lint。
- [x] B. 单元测试和组件测试。
- [x] C. 构建产物。
- [x] D. 安全和依赖扫描。
- [x] E. bundle 体积或性能预算检查。

**解释**：CI 是质量门禁，应覆盖静态质量、测试、构建和关键预算。

### Q3 multiple | CD 发布

发布阶段应考虑哪些能力？

- [x] A. 环境区分和配置注入。
- [x] B. 灰度、回滚和版本追踪。
- [x] C. CDN 上传和缓存刷新。
- [x] D. 部署后冒烟测试和监控。
- [ ] E. 生产部署失败后没有回滚方案。

**解释**：发布不是“上传文件”而已，还要可回滚、可观测、可追踪。

## proj-010

### Q1 single | 大型项目架构

可维护大型前端项目最重要的原则之一是什么？

- [x] A. 模块边界清晰，职责和依赖方向可控。
- [ ] B. 所有代码都放在一个文件里。
- [ ] C. 禁止任何文档。
- [ ] D. 所有状态都挂到 window。

**解释**：大型项目复杂度来自协作、依赖和变化。边界清晰才能可演进。

### Q2 multiple | 架构要素

哪些属于大型前端项目架构要素？

- [x] A. 目录结构和模块分层。
- [x] B. 路由、状态、请求和权限设计。
- [x] C. 组件体系和设计系统。
- [x] D. 测试、监控、规范和发布流程。
- [ ] E. 每个页面都复制一套工具函数。

**解释**：架构不只是目录，更包括运行时能力、工程规范和质量体系。

### Q3 multiple | 状态管理设计

状态管理选型时应考虑什么？

- [x] A. 状态作用域：局部、页面、全局、服务端缓存。
- [x] B. 更新频率和订阅粒度。
- [x] C. 调试、持久化和团队熟悉度。
- [x] D. 是否把服务端数据缓存交给 React Query 等工具。
- [ ] E. 所有状态都必须进入单一全局 store。

**解释**：状态应按来源和作用域拆分，避免全局 store 变成杂物间。

## proj-011

### Q1 single | Rollup 适用场景

Rollup 打包 npm 组件库的常见优势是什么？

- [x] A. 更适合输出 ESM/CJS 等库产物，Tree Shaking 友好。
- [ ] B. 只能打包后端二进制。
- [ ] C. 不能处理任何插件。
- [ ] D. 专门用于数据库迁移。

**解释**：Rollup 常用于库打包，应用打包则更多关注代码分割、开发体验和复杂资源管线。

### Q2 multiple | 组件库配置

打包组件库时，哪些配置值得关注？

- [x] A. 输出 ESM 和 CJS，必要时输出类型声明。
- [x] B. React、Vue 等 peerDependencies 通常不打进库产物。
- [x] C. 配置 external 避免重复打包宿主依赖。
- [x] D. 保留模块结构可提升按需引入体验。
- [ ] E. 把 React 打进每个组件文件里。

**解释**：组件库要避免和宿主应用重复依赖，同时提供清晰的模块入口。

### Q3 multiple | 发布注意

npm 组件库发布前要注意什么？

- [x] A. package.json 的 main/module/types/exports。
- [x] B. sideEffects 标记是否准确。
- [x] C. 样式文件如何引入和按需加载。
- [x] D. 语义化版本和 changelog。
- [ ] E. 发布时提交私钥和 token 到仓库。

**解释**：组件库发布关注入口、类型、样式、副作用和版本兼容。

## proj-012

### Q1 single | 白屏排查

首屏白屏很久，第一步最应该做什么？

- [x] A. 打开 Network 和 Performance，确认慢在网络、资源、JS 执行还是渲染。
- [ ] B. 直接重写项目。
- [ ] C. 删除所有 CSS。
- [ ] D. 只换背景色。

**解释**：白屏可能来自 HTML 慢、资源阻塞、JS 报错、bundle 过大或挂载失败。

### Q2 multiple | 白屏原因

哪些可能导致白屏？

- [x] A. HTML TTFB 高。
- [x] B. 主 JS bundle 过大且同步阻塞。
- [x] C. 入口 JS 报错导致应用未挂载。
- [x] D. CSS 或字体阻塞首屏可见内容。
- [ ] E. 图片设置了 alt。

**解释**：白屏排查要覆盖网络、构建产物、运行时和渲染链路。

### Q3 multiple | 优化手段

哪些手段可优化白屏？

- [x] A. 代码分割和减少首屏 JS。
- [x] B. SSR/SSG 或应用壳。
- [x] C. 内联关键 CSS。
- [x] D. 预加载关键资源并延迟非关键资源。
- [ ] E. 把所有功能都放进入口文件。

**解释**：白屏优化目标是尽快让用户看到有意义内容，同时减少首屏阻塞。

## proj-013

### Q1 single | splitChunks

Webpack splitChunks 的主要作用是什么？

- [x] A. 把公共或异步模块拆分成更合理的 chunk。
- [ ] B. 自动修复所有 JS bug。
- [ ] C. 删除所有 CSS。
- [ ] D. 关闭 Tree Shaking。

**解释**：splitChunks 影响缓存、并发请求、首屏体积和公共依赖复用。

### Q2 multiple | 配置关注点

合理配置 splitChunks 时要关注什么？

- [x] A. chunks 范围：initial、async、all。
- [x] B. minSize、minChunks、cacheGroups。
- [x] C. vendor、common、runtime 的缓存策略。
- [x] D. 避免重复打包公共依赖。
- [ ] E. chunk 越碎越一定越快。

**解释**：chunk 粒度要平衡缓存收益和请求调度成本。

### Q3 multiple | 拆包策略

哪些拆包策略更健康？

- [x] A. 稳定第三方依赖可单独缓存。
- [x] B. 低频重功能路由独立拆分。
- [x] C. 公共代码达到复用价值再抽离。
- [ ] D. 每个小函数都独立 chunk。
- [ ] E. 所有页面共享一个巨大 vendor 且从不评估。

**解释**：拆包不是公式题，要结合用户路径、缓存命中和资源优先级。

## proj-014

### Q1 single | Service Worker 位置

Service Worker 在项目中的角色更接近什么？

- [x] A. 浏览器里的网络代理层，可拦截请求并管理缓存。
- [ ] B. React 组件内部状态。
- [ ] C. 数据库事务。
- [ ] D. CSS 预处理器。

**解释**：SW 可实现离线缓存、预缓存、运行时缓存和推送等能力。

### Q2 multiple | 离线缓存设计

哪些做法正确？

- [x] A. 应用壳资源可预缓存。
- [x] B. 静态 hash 资源可 Cache First。
- [x] C. API 可按新鲜度选择 Network First 或 Stale While Revalidate。
- [x] D. 提供离线 fallback 页面。
- [ ] E. 所有接口数据都永久缓存不更新。

**解释**：离线缓存要按资源类型和数据新鲜度设计。

### Q3 multiple | SW 更新风险

哪些是 SW 常见风险？

- [x] A. 旧 SW 控制页面导致用户拿不到新版本。
- [x] B. 缓存版本清理不当。
- [x] C. skipWaiting/clientsClaim 策略不当导致刷新体验问题。
- [ ] D. SW 不可能影响页面资源加载。
- [ ] E. SW 可以在任意 HTTP 站点使用。

**解释**：SW 需要 HTTPS 安全上下文，更新生命周期和缓存治理必须仔细设计。

## proj-015

### Q1 single | 技术选型

技术选型最不应该只依据什么？

- [ ] A. 业务场景。
- [ ] B. 团队经验。
- [ ] C. 生态和长期维护。
- [x] D. 某个工具最近最热门。

**解释**：选型是约束下的权衡，不是追热点。

### Q2 multiple | React vs Vue 选型维度

以 React/Vue 为例，哪些维度值得评估？

- [x] A. 团队熟悉度和招聘市场。
- [x] B. 生态、组件库、状态管理和工程工具。
- [x] C. 项目复杂度、交互形态和长期维护。
- [x] D. 迁移成本和历史资产。
- [ ] E. 哪个 logo 更好看。

**解释**：框架选型要落到团队、业务、生态和成本，而非单点语法喜好。

### Q3 multiple | 选型落地

好的技术选型方案应包含什么？

- [x] A. 候选方案对比。
- [x] B. 风险和退出机制。
- [x] C. PoC 或小范围验证。
- [x] D. 迁移计划和团队培训。
- [ ] E. 不写任何理由，只凭负责人一句话决定。

**解释**：选型要可解释、可验证、可回滚，尤其是影响长期架构的决策。

## proj-016

### Q1 single | CSR

CSR 的主要特点是什么？

- [x] A. 浏览器下载 JS 后在客户端渲染页面。
- [ ] B. 所有页面构建时生成静态 HTML。
- [ ] C. 每个请求都必须在服务器完整渲染 HTML。
- [ ] D. 不需要浏览器。

**解释**：CSR 首屏可能受 JS 下载和执行影响，但交互丰富、部署简单。

### Q2 multiple | 渲染模式

哪些说法正确？

- [x] A. SSR 每次请求或按需在服务端生成 HTML。
- [x] B. SSG 构建时生成静态 HTML。
- [x] C. ISR 在静态生成基础上按时间或请求增量再生成。
- [x] D. CSR 更依赖客户端 JS 渲染。
- [ ] E. SSR 一定不需要水合。

**解释**：SSR/SSG/ISR/CSR 关注 HTML 生成时机和更新策略，交互页面通常还要 hydration。

### Q3 multiple | 适用场景

哪些匹配合理？

- [x] A. 营销页、文档：SSG 常合适。
- [x] B. 强 SEO 且内容动态：SSR 或 ISR。
- [x] C. 后台管理系统：CSR 常可接受。
- [x] D. 内容更新不频繁但规模大：ISR 可考虑。
- [ ] E. 所有页面都必须 SSR。

**解释**：渲染模式要结合 SEO、更新频率、个性化、成本和交互复杂度。

## proj-017

### Q1 single | Monorepo

Monorepo 的核心是什么？

- [x] A. 多个项目或包放在同一个仓库中统一管理。
- [ ] B. 每个文件一个独立仓库。
- [ ] C. 不允许包之间依赖。
- [ ] D. 只能用于后端项目。

**解释**：Monorepo 方便共享代码、统一规范和原子提交，但需要工具治理。

### Q2 multiple | pnpm + Turborepo

哪些说法正确？

- [x] A. pnpm workspace 管理多包依赖。
- [x] B. pnpm 通过内容寻址和链接机制节省磁盘。
- [x] C. Turborepo 可做任务编排和缓存。
- [x] D. 任务缓存能减少重复构建和测试。
- [ ] E. Monorepo 不需要任何依赖边界管理。

**解释**：Monorepo 的收益依赖依赖图、任务缓存和边界规则。

### Q3 multiple | Monorepo 风险

哪些是 Monorepo 风险？

- [x] A. 依赖边界混乱。
- [x] B. CI 任务变慢。
- [x] C. 版本发布策略复杂。
- [x] D. 权限和所有权治理困难。
- [ ] E. 无法共享任何代码。

**解释**：Monorepo 不是简单把项目搬到一起，需要边界、缓存、发布和所有权治理。

## proj-018

### Q1 single | XSS 与 CSRF

项目安全中，XSS 和 CSRF 的区别是什么？

- [x] A. XSS 是脚本注入执行，CSRF 是借用户身份伪造跨站请求。
- [ ] B. 二者完全相同。
- [ ] C. XSS 只影响服务端数据库。
- [ ] D. CSRF 只影响 CSS。

**解释**：二者威胁模型不同，但可以组合造成更大风险。

### Q2 multiple | XSS 防御实践

哪些做法有助于防 XSS？

- [x] A. 输出按上下文转义。
- [x] B. 富文本使用白名单净化。
- [x] C. CSP 限制脚本执行来源。
- [x] D. Cookie 使用 HttpOnly 降低被读取风险。
- [ ] E. 把用户输入直接塞进 innerHTML。

**解释**：XSS 防御重点是让不可信内容不能作为代码执行。

### Q3 multiple | CSRF 防御实践

哪些做法有助于防 CSRF？

- [x] A. SameSite Cookie。
- [x] B. CSRF Token。
- [x] C. Origin/Referer 校验。
- [x] D. 敏感操作二次确认。
- [ ] E. 只把接口改成 POST。

**解释**：POST 也可能被伪造提交。CSRF 需要组合防御。

## proj-019

### Q1 single | 代码规范体系

团队代码规范体系的核心目标是什么？

- [x] A. 降低协作成本，让风格、质量和流程可自动化约束。
- [ ] B. 让每个人随意写自己喜欢的风格。
- [ ] C. 替代所有架构设计。
- [ ] D. 禁止代码评审。

**解释**：规范不是为了形式统一，而是减少争论、避免低级错误和提高可维护性。

### Q2 multiple | 规范工具链

哪些工具或机制常用于统一规范？

- [x] A. ESLint/Biome。
- [x] B. Prettier 或格式化规则。
- [x] C. TypeScript 严格检查。
- [x] D. Husky/lint-staged/CI 门禁。
- [x] E. Commitlint 和 PR 模板。

**解释**：规范要自动化落地，不能只停留在文档里。

### Q3 multiple | 规范治理

哪些做法有助于规范长期有效？

- [x] A. 规则少而明确，逐步收紧。
- [x] B. 老代码迁移有计划，避免一次性大爆炸。
- [x] C. 规则变更有说明和示例。
- [x] D. CI 中强制执行关键规则。
- [ ] E. 每次评审都靠人工争论缩进。

**解释**：规范治理要减少人工消耗，把机械规则交给工具。

## proj-020

### Q1 single | 10 万条列表

10 万条数据列表渲染性能优化的首选方向是什么？

- [x] A. 虚拟列表，只渲染视口附近元素。
- [ ] B. 一次性渲染所有 DOM。
- [ ] C. 每条数据都创建 iframe。
- [ ] D. 禁用滚动。

**解释**：大列表的主要问题是 DOM 数量、布局和绘制成本。虚拟化能显著降低渲染压力。

### Q2 multiple | 大列表优化

哪些手段合理？

- [x] A. 虚拟滚动。
- [x] B. 分页或无限加载。
- [x] C. 稳定 item key，避免错误复用。
- [x] D. 复杂行组件 memo 化或拆分。
- [ ] E. 每次滚动都重新渲染全部 10 万行。

**解释**：大列表常需要数据分页和视图虚拟化组合处理。

### Q3 multiple | 生产注意

生产大列表还要考虑什么？

- [x] A. 动态高度测量。
- [x] B. 滚动恢复和定位到指定项。
- [x] C. 可访问性和键盘操作。
- [x] D. 筛选、排序和数据更新时的性能。
- [ ] E. 虚拟列表不需要任何加载状态。

**解释**：大列表不只是渲染问题，还涉及交互、数据和可访问体验。

## proj-021

### Q1 single | 单元测试目标

前端单元测试最核心的目标是什么？

- [x] A. 验证小单元逻辑在给定输入下行为正确。
- [ ] B. 替代所有线上监控。
- [ ] C. 只检查 CSS 颜色。
- [ ] D. 手动点完整个系统。

**解释**：单元测试关注函数、组件、hook 等小范围行为，反馈快且定位明确。

### Q2 multiple | React 组件测试

测试 React 组件时，哪些做法合理？

- [x] A. 从用户可见行为出发查询元素。
- [x] B. 模拟点击、输入等用户交互。
- [x] C. 断言 UI 状态和回调结果。
- [x] D. 对网络请求做 mock。
- [ ] E. 只测试内部实现细节和私有变量。

**解释**：组件测试应更接近用户行为，减少对内部实现的脆弱依赖。

### Q3 multiple | Hook 测试

测试自定义 Hooks 时，哪些点重要？

- [x] A. 测试初始状态。
- [x] B. 测试状态更新和副作用。
- [x] C. 正确处理异步更新和 fake timers。
- [x] D. 必要时提供 Provider 包裹上下文。
- [ ] E. Hooks 不能被测试。

**解释**：Hook 是可测试的逻辑单元，但要模拟 React 生命周期和上下文。

## proj-022

### Q1 single | CDN 部署静态资源

静态资源部署 CDN 时，为什么常使用文件内容 hash？

- [x] A. 便于长缓存，同时内容变化后 URL 变化触发更新。
- [ ] B. 为了隐藏文件扩展名。
- [ ] C. 为了让文件更大。
- [ ] D. 为了禁用缓存。

**解释**：内容 hash 是缓存一致性的关键，解决“长缓存”和“及时更新”的冲突。

### Q2 multiple | CDN 部署注意

哪些点需要注意？

- [x] A. HTML 入口和静态资源使用不同缓存策略。
- [x] B. 上传产物后再切流或发布 HTML。
- [x] C. 配置 gzip/brotli、HTTPS 和安全头。
- [x] D. 资源路径 publicPath/base 配置正确。
- [ ] E. 先删除旧资源再上传新资源，且没有回滚。

**解释**：发布顺序错误可能导致 HTML 引用的资源 404。静态资源发布要可回滚。

### Q3 multiple | CDN 缓存问题

哪些问题可能发生？

- [x] A. 缓存未刷新导致用户拿到旧 HTML。
- [x] B. publicPath 错误导致资源加载 404。
- [x] C. 跨域字体缺少 CORS 头。
- [x] D. sourcemap 暴露策略不当。
- [ ] E. CDN 一定保证所有节点同时更新。

**解释**：CDN 是分布式系统，缓存刷新和节点一致性需要监控和策略。

## proj-023

### Q1 single | RBAC

RBAC 的核心是什么？

- [x] A. 用户关联角色，角色关联权限。
- [ ] B. 所有用户共享同一个权限。
- [ ] C. 前端本地决定所有后端权限。
- [ ] D. 用 CSS 控制登录状态。

**解释**：RBAC 通过角色管理权限集合，降低直接给用户配置权限的复杂度。

### Q2 multiple | 前端权限控制

前端实现 RBAC 时，哪些做法正确？

- [x] A. 路由级权限控制。
- [x] B. 菜单和按钮级显示控制。
- [x] C. 接口请求仍由后端做最终鉴权。
- [x] D. 权限数据变更后刷新或重新计算可见能力。
- [ ] E. 隐藏按钮就等于接口安全。

**解释**：前端权限主要改善体验和减少误操作，不能作为安全最终防线。

### Q3 multiple | 权限设计细节

哪些设计更可维护？

- [x] A. 用权限点而不是到处写角色名判断。
- [x] B. 权限配置集中管理。
- [x] C. 支持动态路由和默认 403 页面。
- [x] D. 对权限变化做缓存失效和重新拉取。
- [ ] E. 每个组件随意硬编码用户 ID。

**解释**：权限系统要可演进，最好围绕 permission/action/resource 建模。

## proj-024

### Q1 single | i18n 核心

前端国际化的核心不只是翻译文案，还包括什么？

- [x] A. 日期、数字、货币、复数、方向和资源等本地化。
- [ ] B. 只把中文改成英文。
- [ ] C. 删除所有文本。
- [ ] D. 禁止用户切换语言。

**解释**：i18n 包括语言、格式、布局方向和文化习惯。

### Q2 multiple | React i18n 实践

哪些做法正确？

- [x] A. 文案使用 key 管理，避免散落硬编码。
- [x] B. 按语言包拆分和懒加载。
- [x] C. 使用 ICU MessageFormat 处理变量和复数。
- [x] D. 语言切换后更新 html lang 和必要方向属性。
- [ ] E. 把所有文案拼接成字符串常量，不支持变量。

**解释**：复杂语言场景需要格式化工具，而不是简单字符串替换。

### Q3 multiple | i18n 风险

哪些是国际化常见问题？

- [x] A. 文案长度变化导致布局溢出。
- [x] B. RTL 语言方向和图标镜像。
- [x] C. 日期和时区格式错误。
- [x] D. 翻译缺失的 fallback。
- [ ] E. 所有语言的词长都完全相同。

**解释**：国际化会影响 UI 结构、数据格式和交互细节。

## proj-025

### Q1 single | Zustand 特点

Zustand 相比 Redux 的常见特点是什么？

- [x] A. API 更轻量，基于 store、set/get 和 selector 订阅。
- [ ] B. 必须写 reducer 和 action type。
- [ ] C. 只能用于服务端。
- [ ] D. 不能在 React 中使用。

**解释**：Zustand 更轻量直接，Redux 更强调可预测架构、生态和中间件模式。

### Q2 multiple | Redux 适用

哪些场景 Redux 仍然有优势？

- [x] A. 大团队需要严格数据流规范。
- [x] B. 复杂中间件、时间旅行和调试生态。
- [x] C. 大量业务 action 需要审计和追踪。
- [ ] D. 只有一个局部弹窗状态。
- [ ] E. 所有服务端请求缓存都必须手写进 Redux。

**解释**：Redux 适合复杂、规范化的全局状态，不适合所有小状态。

### Q3 multiple | 状态选型

哪些说法正确？

- [x] A. 服务端状态可考虑 React Query/SWR。
- [x] B. UI 局部状态优先放组件内。
- [x] C. 全局客户端状态才考虑 Zustand/Redux 等。
- [x] D. 状态库选择要看团队和复杂度。
- [ ] E. 一个项目只能有一种状态管理方式。

**解释**：状态按来源和作用域分层管理，工具可以组合使用。

## proj-026

### Q1 single | HMR 目标

Webpack HMR 的目标是什么？

- [x] A. 在不完整刷新页面的情况下替换更新模块，保留部分状态。
- [ ] B. 生产环境自动扩容服务器。
- [ ] C. 删除所有缓存。
- [ ] D. 禁止模块依赖。

**解释**：HMR 提升开发体验，减少刷新和状态丢失。

### Q2 multiple | HMR 流程

HMR 通常涉及哪些步骤？

- [x] A. 开发服务器监听文件变化。
- [x] B. 重新编译变更模块并生成 update manifest/chunk。
- [x] C. 客户端收到更新通知。
- [x] D. runtime 拉取更新并执行 accept 逻辑。
- [ ] E. 浏览器必须关闭再打开。

**解释**：HMR 需要 dev server、runtime 和模块 accept 边界配合。

### Q3 multiple | HMR 边界

哪些情况会影响 HMR 效果？

- [x] A. 模块没有可接受的更新边界时可能退化为整页刷新。
- [x] B. 组件状态能否保留取决于框架和插件支持。
- [x] C. 副作用模块更新后可能需要手动清理。
- [ ] D. HMR 一定能安全替换任何模块。
- [ ] E. HMR 主要用于生产用户访问。

**解释**：HMR 是开发时能力，副作用和更新边界处理不当会导致状态异常。

## proj-027

### Q1 single | localStorage 特点

localStorage 的典型特点是什么？

- [x] A. 同源持久键值存储，同步 API。
- [ ] B. 只能存二进制大文件。
- [ ] C. 请求时自动作为 Cookie 发送。
- [ ] D. 只能在 Service Worker 中访问。

**解释**：localStorage 简单但同步阻塞，容量有限，不适合大量数据或敏感 token。

### Q2 multiple | 存储方案选择

哪些说法正确？

- [x] A. Cookie 适合需要随请求自动携带的小型凭证或偏好。
- [x] B. sessionStorage 随标签页会话结束而清除。
- [x] C. IndexedDB 适合大量结构化数据和离线缓存。
- [x] D. Cache Storage 适合缓存 Request/Response。
- [ ] E. 所有敏感数据都适合长期明文放 localStorage。

**解释**：存储选型要看容量、生命周期、访问方式、安全和是否随请求发送。

### Q3 multiple | 存储限制

前端存储要注意哪些限制？

- [x] A. 容量和浏览器策略差异。
- [x] B. 隐私模式或用户清理数据。
- [x] C. XSS 对可被 JS 读取的数据有威胁。
- [x] D. Cookie 过大影响请求体积。
- [ ] E. IndexedDB 永远不会失败。

**解释**：本地存储不是可靠数据库，必须处理失败、清理、配额和安全问题。

## proj-028

### Q1 single | 项目图片优化

项目图片优化最应该先避免什么？

- [x] A. 按小尺寸展示却下载超大原图。
- [ ] B. 使用现代图片格式。
- [ ] C. 设置宽高占位。
- [ ] D. 使用 CDN。

**解释**：图片尺寸不匹配会直接浪费带宽和解码成本。

### Q2 multiple | 格式选择

哪些格式选择更合理？

- [x] A. 照片类优先 WebP/AVIF 等现代格式并提供 fallback。
- [x] B. 图标可使用 SVG。
- [x] C. 透明复杂位图可考虑 WebP/PNG。
- [x] D. 动图可考虑视频格式替代 GIF。
- [ ] E. 所有图片都必须 BMP。

**解释**：图片格式要按内容类型、透明度、动画和兼容性选择。

### Q3 multiple | 加载策略

哪些加载策略正确？

- [x] A. 首屏关键图优先加载。
- [x] B. 非首屏图懒加载。
- [x] C. 使用 srcset/sizes。
- [x] D. 预留尺寸减少 CLS。
- [ ] E. 所有图片都设置最低优先级。

**解释**：图片加载策略要区分关键与非关键资源。

## proj-029

### Q1 single | 实时推送选型

双向实时消息推送最常用的浏览器技术是什么？

- [x] A. WebSocket。
- [ ] B. localStorage。
- [ ] C. CSS animation。
- [ ] D. HTML meta refresh。

**解释**：WebSocket 提供持久全双工连接，适合聊天、通知和实时状态同步。

### Q2 multiple | WebSocket 重连

连接断开后，重连设计应考虑什么？

- [x] A. 指数退避和最大重试间隔。
- [x] B. 心跳检测连接可用性。
- [x] C. 鉴权过期后的刷新和重连。
- [x] D. 网络恢复和页面可见性变化。
- [ ] E. 每 1ms 无限制重连。

**解释**：重连要避免风暴，同时保证用户恢复在线后能继续接收消息。

### Q3 multiple | 消息可靠性

实时消息系统还要考虑什么？

- [x] A. 消息 ID 去重。
- [x] B. 断线期间消息补偿或拉取历史。
- [x] C. 顺序和幂等处理。
- [x] D. 发送失败重试和 ACK。
- [ ] E. WebSocket 自动保证业务消息永不丢失。

**解释**：WebSocket 只是通道，业务可靠性要自己设计。

## proj-030

### Q1 single | Babel 作用

Babel 主要解决什么问题？

- [x] A. 将较新的 JavaScript 语法转换为目标环境可运行的语法。
- [ ] B. 自动实现所有浏览器 API。
- [ ] C. 压缩图片。
- [ ] D. 部署 CDN。

**解释**：Babel 主要做语法转换，缺失的运行时 API 需要 polyfill。

### Q2 multiple | Polyfill

哪些说法正确？

- [x] A. Polyfill 用于补齐目标环境缺失的 API。
- [x] B. core-js 是常见 polyfill 来源。
- [x] C. 按目标浏览器和实际使用能力引入可减少体积。
- [x] D. Polyfill 可能污染全局或改变运行环境。
- [ ] E. Babel 语法转换一定会自动提供所有 Promise、Map 等 API。

**解释**：语法和 API 是两类兼容问题，不能混淆。

### Q3 multiple | 兼容性治理

项目兼容性应如何治理？

- [x] A. 明确 browserslist。
- [x] B. 基于目标浏览器配置 Babel、PostCSS 和 polyfill。
- [x] C. 做真实浏览器或云测验证。
- [x] D. 监控兼容错误和白屏。
- [ ] E. 不定义目标浏览器，所有问题上线后再说。

**解释**：兼容性是产品决策和工程配置共同决定的，不是单个工具自动完成。

## proj-031

### Q1 single | 前端容器化

前端项目容器化部署时，常见生产镜像更推荐包含什么？

- [x] A. 构建后的静态产物和一个静态资源服务器，如 Nginx。
- [ ] B. 整个 node_modules、源码和开发服务器。
- [ ] C. 用户本地浏览器。
- [ ] D. 数据库 root 密码明文。

**解释**：前端生产容器通常使用多阶段构建，最终镜像只保留运行所需产物。

### Q2 multiple | Dockerfile 实践

哪些做法合理？

- [x] A. 多阶段构建：builder 阶段安装依赖并构建。
- [x] B. runtime 阶段使用轻量镜像承载 dist。
- [x] C. 利用 lockfile 保证依赖可复现。
- [x] D. 使用 .dockerignore 减少构建上下文。
- [ ] E. 把 .env.production 私钥直接写进镜像。

**解释**：镜像要小、可复现、安全，敏感配置应由部署环境注入。

### Q3 multiple | 部署注意

容器化前端部署还要注意什么？

- [x] A. SPA 路由 fallback 到 index.html。
- [x] B. 静态资源缓存头。
- [x] C. 健康检查和日志。
- [x] D. 环境变量是构建时注入还是运行时注入。
- [ ] E. 生产容器必须暴露 Vite dev server。

**解释**：前端容器不只是打包镜像，还要处理路由、缓存、配置和运维可观测性。

## proj-032

### Q1 single | React Query 解决什么

React Query 主要解决哪类状态问题？

- [x] A. 服务端状态的请求、缓存、同步和失效。
- [ ] B. 所有 CSS 样式。
- [ ] C. 浏览器路由渲染。
- [ ] D. Webpack loader 转换。

**解释**：服务端状态和客户端 UI 状态不同，前者需要缓存、重试、刷新和失效机制。

### Q2 multiple | 核心概念

哪些是 React Query 核心概念？

- [x] A. queryKey。
- [x] B. queryFn。
- [x] C. staleTime/cacheTime 或 gcTime。
- [x] D. invalidation。
- [ ] E. CSS selector specificity。

**解释**：queryKey 定义缓存身份，失效和新鲜度策略决定何时重新获取。

### Q3 multiple | 使用注意

哪些做法正确？

- [x] A. queryKey 要稳定且包含影响结果的参数。
- [x] B. mutation 成功后按需 invalidate 或更新缓存。
- [x] C. 区分 loading、error、empty、success 状态。
- [x] D. 避免把所有 UI 局部状态塞进 React Query。
- [ ] E. 所有接口都用同一个 queryKey。

**解释**：错误的 queryKey 会造成缓存串数据或无法复用。

## proj-033

### Q1 single | Vite 插件

Vite 插件和哪套插件机制关系最密切？

- [x] A. Rollup 插件机制。
- [ ] B. jQuery 插件机制。
- [ ] C. Babel preset-env。
- [ ] D. CSS z-index。

**解释**：Vite 插件扩展了 Rollup 插件，增加开发服务器相关钩子。

### Q2 multiple | Vite 插件钩子

哪些是常见 Vite/Rollup 插件钩子或能力？

- [x] A. `config` / `configResolved`。
- [x] B. `resolveId`。
- [x] C. `load`。
- [x] D. `transform`。
- [x] E. `configureServer`。

**解释**：插件可介入配置、解析、加载、转换和开发服务器生命周期。

### Q3 multiple | 插件编写注意

编写 Vite 插件时，哪些做法合理？

- [x] A. 明确插件只在 serve、build 还是两者都生效。
- [x] B. transform 中避免不必要地处理所有文件。
- [x] C. 处理 sourcemap。
- [x] D. 对虚拟模块使用稳定命名约定。
- [ ] E. 在插件里同步阻塞读取整个项目所有文件作为常规操作。

**解释**：插件性能和作用范围会直接影响开发体验和构建速度。

## proj-034

### Q1 single | 动画方案选择

普通 UI 状态过渡最常优先选择什么？

- [x] A. CSS transition/animation。
- [ ] B. 每帧手写 layout。
- [ ] C. 服务端渲染动画。
- [ ] D. SQL 查询。

**解释**：简单 UI 动画用 CSS 声明式实现成本低，复杂交互再考虑 JS 动画库或 Canvas。

### Q2 multiple | 动画方案

哪些动画方案和场景匹配合理？

- [x] A. CSS transition：hover、展开、淡入淡出等简单状态。
- [x] B. Web Animations API：需要 JS 控制但仍使用浏览器动画引擎。
- [x] C. Canvas/WebGL：大量粒子或复杂绘制。
- [x] D. JS + rAF：高度自定义的逐帧控制。
- [ ] E. 所有动画都用 `setInterval(1ms)`。

**解释**：动画方案要根据复杂度、性能、可控性和维护成本选择。

### Q3 multiple | 动画性能

哪些做法有助于动画性能？

- [x] A. 优先 transform/opacity。
- [x] B. 避免大面积 filter 和阴影动画。
- [x] C. 减少同时动画元素数量。
- [x] D. 支持 `prefers-reduced-motion`。
- [ ] E. 给所有元素永久 `will-change`。

**解释**：动画优化要关注渲染阶段、图层成本和用户动态效果偏好。

## proj-035

### Q1 single | Module Federation

Webpack Module Federation 的核心能力是什么？

- [x] A. 运行时在多个构建之间共享和加载模块。
- [ ] B. 只负责 CSS 预处理。
- [ ] C. 替代 HTTP 协议。
- [ ] D. 自动生成数据库表。

**解释**：模块联邦让应用可以暴露和消费远程模块，并共享依赖。

### Q2 multiple | 模块联邦 vs qiankun

哪些说法正确？

- [x] A. Module Federation 更偏模块级运行时共享。
- [x] B. qiankun 更偏应用级微前端加载和沙箱隔离。
- [x] C. 模块联邦需要处理共享依赖版本兼容。
- [x] D. qiankun 需要处理样式隔离和全局变量污染。
- [ ] E. 二者完全没有任何微前端关系。

**解释**：两者解决层级不同，可以独立使用，也可能组合。

### Q3 multiple | 模块联邦风险

使用 Module Federation 时要注意什么？

- [x] A. 远程模块不可用时的降级。
- [x] B. shared 依赖版本冲突。
- [x] C. 部署版本兼容和缓存。
- [x] D. 类型声明和本地开发体验。
- [ ] E. 远程模块一定和宿主同步发布。

**解释**：模块联邦提高运行时灵活性，也增加部署和依赖兼容复杂度。

## proj-036

### Q1 single | Sentry Source Map

Sentry 关联 Source Map 的主要目的是什么？

- [x] A. 将压缩后的线上错误栈还原到原始源码位置。
- [ ] B. 加密用户密码。
- [ ] C. 优化 DNS。
- [ ] D. 删除所有错误。

**解释**：生产代码通常被压缩和混淆，Source Map 让错误可定位到源码。

### Q2 multiple | Sentry 集成

集成 Sentry 时，哪些做法正确？

- [x] A. 设置 release 版本并上传对应 sourcemap。
- [x] B. 上报环境、路由、用户匿名标识等上下文。
- [x] C. 过滤敏感字段。
- [x] D. 配置采样和 ignore 规则。
- [ ] E. 把 sourcemap 公开暴露且不做访问控制是唯一方案。

**解释**：Source Map 可以上传到 Sentry 而不公开给所有用户访问。

### Q3 multiple | 错误治理

错误监控落地后还要做什么？

- [x] A. 按版本、影响用户数和频率聚合。
- [x] B. 设置告警阈值。
- [x] C. 将 issue 分派到负责人并跟踪修复。
- [x] D. 结合 breadcrumbs 还原操作路径。
- [ ] E. 收到错误后只截图保存，不处理。

**解释**：监控的价值在闭环治理，而不是堆积错误日志。

## proj-037

### Q1 single | GraphQL 优势

GraphQL 相比 REST 的一个典型优势是什么？

- [x] A. 客户端可以声明需要的字段，减少 over-fetching/under-fetching。
- [ ] B. 不需要任何服务端。
- [ ] C. 自动防所有安全攻击。
- [ ] D. 只能返回字符串。

**解释**：GraphQL 的查询语言让客户端按需获取图状数据。

### Q2 multiple | GraphQL 实践

哪些是 GraphQL 项目实践要点？

- [x] A. 设计 schema 和类型。
- [x] B. 使用 resolver 获取数据。
- [x] C. 处理权限和字段级鉴权。
- [x] D. 通过 DataLoader 等方式避免 N+1 查询。
- [ ] E. 所有查询都允许无限深度嵌套。

**解释**：GraphQL 灵活但需要复杂度限制、权限和性能治理。

### Q3 multiple | GraphQL 风险

哪些是 GraphQL 常见风险？

- [x] A. 查询复杂度过高。
- [x] B. N+1 查询。
- [x] C. 缓存比 REST URL 缓存更复杂。
- [x] D. schema 演进和客户端兼容。
- [ ] E. GraphQL 无法表达任何嵌套数据。

**解释**：GraphQL 不是 REST 的无成本替代，治理能力很关键。

## proj-038

### Q1 single | Long Task

页面明显卡顿时，主线程 Long Task 通常指什么？

- [x] A. 连续占用主线程较久、阻塞输入和渲染的任务。
- [ ] B. 图片文件名太长。
- [ ] C. CSS 类名太短。
- [ ] D. HTTP 301。

**解释**：Long Task 会拖慢交互响应，直接影响 INP。

### Q2 multiple | 排查卡顿

哪些工具和方法有助于排查卡顿？

- [x] A. DevTools Performance 火焰图。
- [x] B. Long Task API 或性能监控。
- [x] C. React Profiler。
- [x] D. 观察 Event Timing/INP 数据。
- [ ] E. 只看页面标题。

**解释**：卡顿要定位到具体任务、调用栈和交互路径。

### Q3 multiple | 优化 Long Task

哪些做法可优化 Long Task？

- [x] A. 任务切片并让出主线程。
- [x] B. 使用 Web Worker 处理重计算。
- [x] C. 减少同步 JSON 解析和大循环。
- [x] D. 虚拟列表减少一次性 DOM 工作。
- [ ] E. 把所有计算都放到点击事件里同步完成。

**解释**：减少单次连续阻塞是提升交互响应的关键。

## proj-039

### Q1 single | Web Font 性能问题

Web Font 可能导致哪些性能问题？

- [x] A. FOIT/FOUT 和字体替换造成的布局偏移。
- [ ] B. SQL 注入。
- [ ] C. DNS 失效。
- [ ] D. HMR 失败。

**解释**：字体会影响文本可见、排版宽度和 CLS。

### Q2 multiple | 字体优化

哪些做法正确？

- [x] A. 使用 WOFF2。
- [x] B. 子集化和 unicode-range。
- [x] C. 合理设置 font-display。
- [x] D. 只 preload 首屏关键字体。
- [ ] E. preload 所有字体所有字重。

**解释**：字体资源要按首屏和字符集加载，避免过度抢占带宽。

### Q3 multiple | 字体稳定性

哪些做法能降低字体造成的 CLS？

- [x] A. 选择度量接近的 fallback 字体。
- [x] B. 使用 size-adjust 等 metric override。
- [x] C. 控制字体加载策略和替换时机。
- [ ] D. 删除所有文本。
- [ ] E. 不设置任何 fallback。

**解释**：字体替换时宽高变化越小，布局越稳定。

## proj-040

### Q1 single | 前端 DDD

前端领域驱动设计更关注什么？

- [x] A. 用业务领域模型组织复杂业务代码和边界。
- [ ] B. 把所有代码按文件类型平铺。
- [ ] C. 只讨论 CSS 命名。
- [ ] D. 删除业务概念。

**解释**：前端 DDD 适合复杂业务系统，让代码结构贴近业务语言和领域边界。

### Q2 multiple | 前端 DDD 落地

哪些做法符合前端 DDD 思路？

- [x] A. 按领域或业务能力划分模块。
- [x] B. 区分 domain、application、infrastructure、ui 等层次。
- [x] C. 用统一业务语言命名模型和用例。
- [x] D. 通过适配层隔离接口和外部服务变化。
- [ ] E. 所有请求直接散落在组件点击事件中。

**解释**：DDD 让业务规则有归属，减少 UI、接口和领域逻辑混杂。

### Q3 multiple | DDD 适用边界

哪些判断正确？

- [x] A. 简单活动页不一定需要完整 DDD。
- [x] B. 复杂中后台、交易、权限、流程系统更可能受益。
- [x] C. 过度分层会增加简单需求成本。
- [ ] D. DDD 能自动替代所有测试。
- [ ] E. DDD 只适用于后端，前端绝对不能借鉴。

**解释**：DDD 是复杂度管理工具，要在业务复杂度足够时使用。

## proj-041

### Q1 single | 低代码编辑器核心

低代码/可视化页面编辑器的前端核心模型通常是什么？

- [x] A. 页面 schema 描述组件树、属性、样式、事件和数据源。
- [ ] B. 把所有页面截图保存成图片。
- [ ] C. 每次编辑都手写 HTML 文件。
- [ ] D. 只靠 localStorage 随机存字符串。

**解释**：低代码平台的关键是用结构化 schema 表达页面，并支持渲染、编辑和发布。

### Q2 multiple | 编辑器架构

哪些模块常见于低代码编辑器？

- [x] A. 画布渲染器。
- [x] B. 组件物料协议和物料市场。
- [x] C. 属性面板和样式编辑器。
- [x] D. 事件、数据源和动作编排。
- [x] E. 撤销重做和历史记录。

**解释**：可视化编辑器是复杂交互系统，核心不仅是拖拽，还包括协议和状态管理。

### Q3 multiple | 低代码风险

哪些是低代码平台常见难点？

- [x] A. schema 版本迁移。
- [x] B. 自定义扩展能力和安全沙箱。
- [x] C. 运行时性能和产物体积。
- [x] D. 协作、权限和发布审批。
- [ ] E. 低代码可以完全不需要工程治理。

**解释**：低代码降低页面搭建门槛，但平台本身需要更强的工程和治理能力。

## proj-042

### Q1 single | JWT 存储安全

前端存储 JWT 时，最需要权衡哪两类风险？

- [x] A. XSS 读取风险和 CSRF 自动携带风险。
- [ ] B. CSS 优先级和 z-index。
- [ ] C. 图片大小和字体大小。
- [ ] D. Git 分支名和提交信息。

**解释**：localStorage 易受 XSS 读取，Cookie 自动携带则要处理 CSRF。

### Q2 multiple | Token 刷新机制

哪些设计更合理？

- [x] A. Access token 短有效期。
- [x] B. Refresh token 更长有效期并安全存储。
- [x] C. 刷新 token 轮换和复用检测。
- [x] D. 多请求同时 401 时合并刷新，避免并发刷新风暴。
- [ ] E. Access token 永久有效。

**解释**：刷新机制要降低泄露影响，并处理并发和失效边界。

### Q3 multiple | 使用 Token 注意

哪些做法正确？

- [x] A. 请求拦截器可统一附带 Authorization。
- [x] B. 退出登录时清理本地凭证并通知服务端撤销。
- [x] C. 权限变化后让旧 token 失效或刷新权限声明。
- [ ] D. JWT payload 可以放用户密码明文。
- [ ] E. 前端解码 JWT 就代表服务端已经验证。

**解释**：JWT payload 默认不加密，可信性来自服务端验证签名和声明。

## proj-043

### Q1 single | 环境配置

前端多环境配置最重要的原则是什么？

- [x] A. 区分构建时配置和运行时配置，避免把秘密写进前端产物。
- [ ] B. 所有环境共用生产接口。
- [ ] C. 把所有密钥写进 git。
- [ ] D. 每次部署手动改源码。

**解释**：前端代码是可被用户下载的，不能包含真正的服务端秘密。

### Q2 multiple | 多环境管理

哪些做法合理？

- [x] A. 使用 `.env` 模板和环境变量前缀控制暴露范围。
- [x] B. 开发、测试、预发、生产配置分离。
- [x] C. 运行时配置可通过静态 config.json 或服务端注入。
- [x] D. CI/CD 根据环境注入构建参数。
- [ ] E. 在代码里硬编码所有环境 URL。

**解释**：多环境配置要可审计、可自动化、可避免误连生产。

### Q3 multiple | 配置风险

哪些是多环境常见事故？

- [x] A. 测试包连到生产接口。
- [x] B. source map 或调试开关在生产误开启。
- [x] C. 构建时变量修改后未重新构建导致不生效。
- [x] D. 密钥误以为前端环境变量是安全的。
- [ ] E. 使用环境变量一定不会出错。

**解释**：环境配置要有校验和发布门禁，前端环境变量不是秘密存储。

## proj-044

### Q1 single | 泄漏排查核心

排查前端内存泄漏最关键的是找到什么？

- [x] A. 阻止对象被回收的引用链。
- [ ] B. 页面背景色。
- [ ] C. HTTP 状态码。
- [ ] D. CSS 文件行数。

**解释**：垃圾回收基于可达性。只要引用链存在，对象就不会被回收。

### Q2 multiple | 常见泄漏场景

哪些是常见泄漏？

- [x] A. 未移除事件监听。
- [x] B. 未清理定时器、订阅、Observer。
- [x] C. 闭包持有大对象。
- [x] D. detached DOM 被 JS 引用。
- [ ] E. 使用 HTML 语义标签。

**解释**：泄漏多来自生命周期结束后资源没有释放。

### Q3 multiple | 修复策略

哪些修复策略合理？

- [x] A. 在组件卸载时清理副作用。
- [x] B. 缓存设置容量和过期策略。
- [x] C. 避免全局变量长期持有页面对象。
- [x] D. 使用 heap snapshot 对比验证修复。
- [ ] E. 每次泄漏都让用户刷新页面解决。

**解释**：刷新只能临时释放，不能修复泄漏根因。

## proj-045

### Q1 single | Feature Flag

Feature Flag 的主要作用是什么？

- [x] A. 在不重新发布代码的情况下控制功能开关、灰度和实验。
- [ ] B. 自动压缩图片。
- [ ] C. 替代数据库。
- [ ] D. 禁止用户登录。

**解释**：功能开关用于灰度发布、A/B 测试、紧急关闭和权限分组。

### Q2 multiple | A/B 测试设计

哪些做法正确？

- [x] A. 用户分桶要稳定。
- [x] B. 实验指标和成功标准提前定义。
- [x] C. 避免多个实验相互污染。
- [x] D. 记录曝光和转化事件。
- [ ] E. 实验结果只看个人感觉。

**解释**：A/B 测试要统计有效，必须有稳定分流、指标和数据质量。

### Q3 multiple | 开关治理

功能开关长期治理要注意什么？

- [x] A. 开关有负责人和过期时间。
- [x] B. 移除已完成实验和废弃开关。
- [x] C. 服务端和前端对开关默认值一致。
- [x] D. 关键开关变更要有审计。
- [ ] E. 永远保留所有历史开关。

**解释**：开关不治理会变成隐藏复杂度，增加测试和维护成本。

## proj-046

### Q1 single | 流式文字输出

类似 ChatGPT 的流式文字输出，本质上前端在做什么？

- [x] A. 接收增量数据并逐步追加到 UI。
- [ ] B. 一次性等完整响应后再隐藏。
- [ ] C. 每个字都刷新整个页面。
- [ ] D. 用 CSS 随机生成文本。

**解释**：流式输出改善感知延迟，让用户更早看到生成过程。

### Q2 multiple | SSE 流式处理

使用 SSE 做流式输出时，哪些说法正确？

- [x] A. 服务端发送 `text/event-stream`。
- [x] B. 客户端可用 EventSource 接收事件。
- [x] C. 可用事件 id 或业务游标处理断线续传。
- [x] D. 需要处理结束标记、错误和取消。
- [ ] E. SSE 原生支持客户端在同一连接上传二进制流。

**解释**：SSE 是服务端到客户端的文本事件流，适合生成文本、日志和进度。

### Q3 multiple | 打字效果体验

流式 UI 还要考虑什么？

- [x] A. 增量解析 Markdown 时避免频繁重排过重。
- [x] B. 滚动跟随和用户手动滚动的冲突。
- [x] C. 中断、重试和恢复。
- [x] D. 内容安全过滤和错误状态。
- [ ] E. 每个 token 都同步强制 layout。

**解释**：流式体验的难点不只是接收数据，还有渲染节奏、滚动和异常控制。

## proj-047

### Q1 single | Session Replay

用户行为录制与回放的核心是什么？

- [x] A. 记录 DOM 结构变化、用户输入和交互事件，再在播放器中重建过程。
- [ ] B. 录制用户摄像头。
- [ ] C. 保存数据库 binlog。
- [ ] D. 只记录 HTTP 状态码。

**解释**：rrweb 一类工具主要记录页面 DOM 快照、增量变更和输入事件。

### Q2 multiple | rrweb 原理

哪些说法正确？

- [x] A. 初始全量快照记录页面结构。
- [x] B. MutationObserver 记录 DOM 增量变化。
- [x] C. 记录鼠标、滚动、输入等交互事件。
- [x] D. 回放端按时间顺序重建状态。
- [ ] E. rrweb 必须录制真实视频流。

**解释**：Session Replay 通常是结构化事件回放，不是视频录屏。

### Q3 multiple | 隐私与性能

实现行为录制要注意什么？

- [x] A. 输入内容脱敏。
- [x] B. 敏感区域屏蔽。
- [x] C. 采样和压缩，控制上报体积。
- [x] D. 用户授权和合规要求。
- [ ] E. 记录所有密码明文最有助于排查。

**解释**：回放系统非常敏感，隐私合规和性能开销必须优先设计。

## proj-048

### Q1 single | Babel 插件核心

Babel 插件的核心处理对象是什么？

- [x] A. AST。
- [ ] B. TCP 包。
- [ ] C. 图片像素。
- [ ] D. DNS 记录。

**解释**：Babel 解析源码为 AST，插件通过 visitor 遍历和修改 AST。

### Q2 multiple | 插件流程

编写自动添加函数执行时间日志的 Babel 插件，通常会做什么？

- [x] A. 访问 FunctionDeclaration/FunctionExpression/ArrowFunctionExpression 等节点。
- [x] B. 在函数体前后插入计时代码。
- [x] C. 注意 async、return、throw 等控制流。
- [x] D. 使用 @babel/types 创建 AST 节点。
- [ ] E. 直接用正则替换所有大括号就足够可靠。

**解释**：AST 转换要理解语法结构和控制流，正则替换很容易破坏代码。

### Q3 multiple | 插件注意事项

Babel 插件还要注意什么？

- [x] A. 避免重复插桩。
- [x] B. 保留 sourcemap。
- [x] C. 控制只在需要的环境生效。
- [x] D. 避免影响函数 this、arguments 和返回值语义。
- [ ] E. 插件可以随意改变业务逻辑而无需测试。

**解释**：编译期插桩必须保证语义等价和可调试。

## proj-049

### Q1 single | BFF

BFF 的核心定位是什么？

- [x] A. 面向特定前端体验聚合和裁剪后端能力的中间层。
- [ ] B. 浏览器缓存策略。
- [ ] C. CSS 预处理器。
- [ ] D. 图片格式。

**解释**：BFF 为不同前端端侧提供更贴合页面的数据接口和编排能力。

### Q2 multiple | 为什么需要 BFF

哪些场景适合引入 BFF？

- [x] A. 前端需要聚合多个后端服务。
- [x] B. 不同端需要不同数据形态。
- [x] C. 需要在服务端处理鉴权、裁剪和适配。
- [x] D. 减少前端多接口瀑布请求。
- [ ] E. 项目没有任何接口也必须引入 BFF。

**解释**：BFF 解决前端体验和后端服务粒度不匹配的问题。

### Q3 multiple | BFF 风险

哪些是 BFF 的风险？

- [x] A. 变成新的业务单体和瓶颈。
- [x] B. 职责边界不清导致逻辑重复。
- [x] C. 缓存、错误处理和降级复杂。
- [x] D. 多团队协作和接口契约管理成本。
- [ ] E. BFF 会自动消除所有后端复杂度。

**解释**：BFF 是架构层权衡，需要清晰边界和运维能力。

## proj-050

### Q1 single | 性能预算

性能预算的作用是什么？

- [x] A. 给体积、指标或资源数量设定上限，防止性能持续退化。
- [ ] B. 管理公司财务报销。
- [ ] C. 自动修复所有慢接口。
- [ ] D. 替代需求评审。

**解释**：性能预算让性能成为可执行的工程约束，而不是上线后才发现变慢。

### Q2 multiple | 预算指标

哪些可以作为性能预算？

- [x] A. 首屏 JS gzip 体积。
- [x] B. LCP/INP/CLS 阈值。
- [x] C. 图片总量和单资源大小。
- [x] D. 第三方脚本数量。
- [ ] E. 变量命名长度。

**解释**：预算应和用户体验和加载成本直接相关，并可自动检测。

### Q3 multiple | CI 强制执行

如何在 CI 中执行性能预算？

- [x] A. 构建后检查 bundle size。
- [x] B. Lighthouse CI 或自定义性能脚本跑关键页面。
- [x] C. 超出阈值时阻断合并或要求说明。
- [x] D. PR 中展示体积差异。
- [ ] E. 只在口头上提醒大家注意性能。

**解释**：预算必须自动化、可见、可追踪，才不会被日常需求慢慢冲掉。
